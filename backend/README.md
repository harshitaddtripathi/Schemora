# Schemora Backend API

FastAPI backend service for Schemora scheme discovery and verification engine.

## Execution

```bash
uv sync
uv run pytest
uv run uvicorn app.main:app --host 0.0.0.0 --port 8000 --reload
```
